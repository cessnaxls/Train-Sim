# WorldRail Self-Contained
Clean-room replacement. All 3D scene textures are bundled under public/assets. The Leaflet route guide is converted into the actual 3D spline. Render build: `npm install && npm run build`; publish `dist`.

I could not run npm install in the ChatGPT execution environment because registry access timed out, so this package is not falsely labeled build-verified.
